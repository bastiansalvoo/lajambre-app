Actúa como un desarrollador Senior en React Native (Expo) y ayúdame a refactorizar la interfaz de usuario de mi aplicación frontend.

OBJETIVO:
Reemplazar todas las instancias del componente nativo `Alert.alert` de 'react-native' por la librería personalizada `react-native-toast-message`.

ALCANCE:
Analiza todos los archivos `.tsx` y `.ts` dentro del directorio `frontend/app/` y `frontend/src/`.
Excluye de este refactoring el archivo `frontend/app/_layout.tsx` (ya que ahí reside la configuración global que no debe ser tocada).

INSTRUCCIONES DE REFACTORING:
1. Gestión de Importaciones:
   - Elimina `Alert` de las importaciones de `react-native` en los archivos donde se utilice.
   - Si `Alert` era lo único que se importaba de `react-native`, elimina la línea completa.
   - Añade la importación `import Toast from 'react-native-toast-message';` en la parte superior de los archivos modificados.

2. Transformación de Código:
   - Encuentra cada bloque de código que utilice la sintaxis: `Alert.alert('Titulo', 'Mensaje')` o `Alert.alert('Titulo', 'Mensaje', [botones])`.
   - Reemplázalo por la siguiente estructura:
     Toast.show({
       type: 'determinar_tipo',
       text1: 'Titulo',
       text2: 'Mensaje'
     });

3. Lógica Condicional para el 'type' del Toast:
   - Infiere el tipo de alerta basándote en el Título o el contexto del mensaje.
   - Usa `type: 'error'` si el título o mensaje contiene palabras como: "Error", "Insuficiente", "Denegado", "Fallo", "Rechazado", "Identifícate".
   - Usa `type: 'success'` si el título o mensaje contiene palabras como: "Éxito", "Gratis", "Confirmado", "Completado".
   - Si el `Alert.alert` original tenía un arreglo de botones (ej. un botón para "Cancelar" y otro para "Ir al Login" o "Eliminar"), NO lo cambies a Toast automáticamente. Añade un comentario TODO `// TODO: Refactorizar Alert interactiva` arriba de esa línea, ya que los Toasts no son modales interactivos y requieren revisión manual.

Ejecuta el escaneo y aplica los cambios en el código directamente.