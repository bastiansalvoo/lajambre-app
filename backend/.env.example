# ── BASE DE DATOS ──
DATABASE_URL="postgresql://USUARIO:PASSWORD@localhost:5433/lajambre_db?schema=public"

# ── SEGURIDAD ──
JWT_SECRET="genera_un_secreto_de_64_caracteres_aqui"
# CORS_ORIGIN="https://tudominio.cl"

# ── HOST ──
API_HOST="192.168.0.23"
PORT=3000
HOST=0.0.0.0

# ── WEBPAY (Transbank) ──
WEBPAY_COMMERCE_CODE="TU_CODIGO_COMERCIO"
WEBPAY_API_KEY="TU_API_KEY"
WEBPAY_ENVIRONMENT="Integration"  # Cambiar a "Production" cuando estés listo
WEBPAY_RETURN_URL="http://192.168.0.23:3000/orders/webpay/confirm"

# ── EMAIL (SMTP) ──
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="tu_email@gmail.com"
SMTP_PASS="tu_app_password"
SMTP_FROM="\"La Jambre App\" <no-reply@lajambre.cl>"
